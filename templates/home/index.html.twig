{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
        .btn {
            background-color: #4CAF50;
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 10px;
        
        }
        a {
            color: white;
            text-decoration: none;
        }
	</style>

	<div class="example-wrapper">
		<h1>Bienvenue sur le blog des débutants en symfony ✅</h1>

		Tu apprendras plein de choses ici, c'est promis! 🚀:
		{% for post in posts %}
			<h2>{{ post.title }}</h2>
			<p>{{ post.summary }}</p>
			<button class="btn">
				<a href="{{ path('show', {slug: post.slug, id: post.id}) }}">Lire la suite</a>
			</button>
            <time>Crée le {{ post.createdAt|date('d/m/Y') }}</time>
		{% endfor %}
	</div>
{% endblock %}
